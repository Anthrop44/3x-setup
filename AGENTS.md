Read `README.md`, `doc.md`, `remote/constants.schema.json`, and `remote/config.schema.json` first.

Modify `README.md`, `README.zh-Hans.md`, and `doc.md` to reflect actual situation after modifying features.

If you mess up user's proxy server, consider asking user to rebuild it to start a fresh installation as these scripts are not designed to be idempotent.
